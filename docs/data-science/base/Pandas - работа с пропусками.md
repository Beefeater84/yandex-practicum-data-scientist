
2026-03-03 21:42
tags: #python



```python

# Вывод абсолютного значения пропусков
df.isna().sum()

# Вывод доли пропусков
empty = df.isna().mean()
# Вывод только колонок где пропуски есть
empty.loc[empty > 0]

# Вывод доли пропусков в %
empty = df.isna().mean() * 100
# Вывод только колонок где пропуски есть
empty.loc[empty > 0].round(2)

```


---
## Links
Оценка пропусков часто делается в предварительном анализе данных: [[Pandas - анализ Data Frame]]