2026-03-03 21:54
tags: #python

```python

# Выводим столюцы и типы данных
df.info()

# Выводим (количество строк, количество колонок)
df.shape

# Выводим 5 рандомных значений для анализа
df.sample(5, random_state=42)
```

```python

# Запоминаем категорияальные ячейки, чтобы их выводить
category_cols = ['device_type_canonical']
# Выводим категориальные колонки в строку
print('Категориальные колонки:', ', '.join(category_cols))
# Выводим категориальные колонки в строку, если они индексы
print('Категориальные колонки:', ', '.join(map(str, category_cols)))

# Выводим клонки в столбик
print('Категориальные колонки:')
for column in category_cols:
	print(column)
	

## Вывод размера DF
print(f'Количество строк: {df.shape[0]}') 
print(f'Количество колонок: {df.shape[1]}')

```

## Оценка пропусков
[[Pandas - работа с пропусками]]
